spring:
  application.name: pet-tracker
  profiles:
    active: h2
  threads:
    virtual:
      enabled: true

---
spring:
  config:
    activate:
      on-profile: h2
  datasource:
    url: jdbc:h2:mem:pet_tracker_db
    driver-class-name: org.h2.Driver
    username: sa
    password:
  jpa:
    database: h2
    show-sql: true
    hibernate:
      ddl-auto: update
  h2:
    console:
      enabled: true
  data:
    mongodb:
      password: a
      username: mongo
      host: localhost
      database: tracker
      port: 27017
de:
  flapdoodle:
    mongodb:
      embedded:
        version: 7.0.4
---
spring:
  config:
    activate:
      on-profile: test
  sql:
    init:
      mode: never
  jpa:
    show-sql: true